<% provide :title, t(".edit_title") %>
<% provide :button_text, t(".button_submit") %>

<h1><%= t ".edit_title" %></h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= render "form" %>

    <div class="gravatar_edit">
      <%= gravatar_for @user %>
      <%= link_to t(".gavartar_edit"), Settings.user.gravatar.edit, target: "_blank" %>
    </div>
  </div>
</div>
